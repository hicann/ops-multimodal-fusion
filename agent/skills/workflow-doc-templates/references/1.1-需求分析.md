# {算子名称} 需求分析

> 1.1 需求分析阶段产出。核对确认项，用户未提及的通过问卷补齐。作为后续 Spec、方案、测试的输入。

## 1. 数学定义

```
// 输入输出
输入: {x - 输入张量, shape, dtype; y - 输入张量, shape, dtype（二元算子）}
输出: {z - 输出张量, shape, dtype}

// 数学公式
{如 elementwise 二元：z = x + y；一元：y = f(x)（如 y = abs(x)）}
```

### 操作说明

| 参数 | 说明 |
|------|------|
| x | 输入张量 |
| y | 输入张量（二元算子；一元算子省略） |
| z | 输出张量 |
| {attr} | 属性参数（如 kernel_size / dim / eps，视算子而定） |

## 2. 算子规格

### 2.1 基本信息

| 项目 | 内容 |
|------|------|
| 算子名称 | `{op}`（`torch.ops.ops_multimodal_fusion.{op}`） |
| 算子类别 | {elementwise 数学 / pooling / normalization / reduction / complex / ...} |
| 需求类型 | {特定用例（指定 shape/dtype） / 通用} |
| 代码目录 | `applications/llm/{op}/arch35/` |
| 测试目录 | `tests/{op}/` |

### 2.2 接口原型

Schema（`.asc` 内 `TORCH_LIBRARY_FRAGMENT` 声明）：

```cpp
m.def("{op}(Tensor x, Tensor y) -> Tensor");
```

Python 调用口径：

```python
torch.ops.ops_multimodal_fusion.{op}(x.npu(), y.npu()).cpu()
```

### 2.3 参数规格

| 参数 | 方向 | 类型 | 说明 |
|------|------|------|------|
| x | 输入 | Tensor | 输入张量 |
| y | 输入 | Tensor | 输入张量（二元算子；一元算子省略） |
| {attr} | 输入 | {int / float / int[] / bool} | {属性参数，如 kernel_size / dim} |
| z | 输出 | Tensor | 输出张量 |

### 2.4 参数约束

| 参数 | 约束 |
|------|------|
| x | 非空 Tensor，device 为 npu |
| x / y shape | 二元算子要求 `x.sizes() == y.sizes()` |
| dtype | 属于支持数据类型列表 |
| {attr} | {属性取值范围，如 kernel_size > 0} |

### 2.5 支持数据类型

| dtype | 是否支持 | 说明 |
|-------|---------|------|
| float32 | | |
| float16 | | |
| int32 | | |

### 2.6 目标芯片与架构

| 项目 | 内容 |
|------|------|
| 目标芯片 | {ascend950 / ascend910b / ascend910_93} |
| 目标架构 | {arch35（dav-3510，默认）/ arch22（dav-2201）} |
| CANN 版本 | 9.0.0 |

### 2.7 精度要求

| dtype | rtol | atol | 说明 |
|-------|------|------|------|
| float32 | 1e-4 | 1e-5 | torch.allclose |
| float16 | 1e-3 | 1e-3 | torch.allclose |
| int32 | 位精确 | 位精确 | torch.equal |

### 2.8 性能预期

{性能目标或"无明确要求"}

## 3. 可行性评估

### 3.1 API 可用性

{Ascend C API 可用性分析：所需矢量/标量指令（如 AscendC::Add / Abs / 归约）是否可用}

### 3.2 性能可行性

{预估的 AI Core 利用率和性能瓶颈}

### 3.3 风险点

| 风险 | 影响 | 缓解措施 |
|------|------|---------|

## 4. 其他要求

{用户指定的实现方案约束，如要求 class 化 kernel、指定 tiling 切分策略；无则填"无"}

## 5. 用户原始需求

<!-- 逐条记录，不加工、不裁剪，作为下游对照的根本依据 -->

| # | 需求内容 |
|---|----------|
| 1 | |
